export interface Config {
    clientID: string;
    guildID: string;
    DatabaseName: string;
    CollectionName: string;
}

export interface Credentials {
    DiscordToken: string;
    MongoDB: string;
}
